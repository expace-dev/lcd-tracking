<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">

		{# Prélaunch #}
		<meta name="robots" content="noindex, nofollow">

		<title>LCD Tracking —
			{% block title %}Bienvenue
			{% endblock %}
		</title>

		<meta name="description" content="{% block meta_description %}LCD Tracking formalise les validations clés de fin de séjour et de ménage pour protéger propriétaires et intervenants, avec une trace factuelle.{% endblock %}">

		{# Canonical #}
		<link rel="canonical" href="{{ app.request.uri }}">

		{# OpenGraph #}
		<meta property="og:site_name" content="LCD Tracking">
		<meta property="og:title" content="LCD Tracking — {{ block('title')|striptags|trim }}">
		<meta property="og:description" content="{{ block('meta_description')|striptags|trim }}">
		<meta property="og:type" content="website">
		<meta property="og:url" content="{{ app.request.uri }}">

		{# À activer quand tu as une image #}
		{# <meta property="og:image" content="{{ absolute_url(asset('images/og.jpg')) }}"> #}

		{# Twitter (optionnel) #}
		<meta name="twitter:card" content="summary_large_image"> {# Favicons (minimum) #}
		<link rel="icon" href="{{ asset('favicon.ico') }}">

		{% block stylesheets %}
			{{ encore_entry_link_tags('app') }}
		{% endblock %}

		{% block javascripts %}
			{{ encore_entry_script_tags('app') }}
		{% endblock %}
	</head>
	<body>
		<a class="skip-link" href="#main">Aller au contenu</a>

		<header class="site-header">
			{{ include('partials/_navbar.html.twig') }}
		</header>

		<main id="main" class="site-main">
			{% if show_header is defined and show_header %}
				<section class="section section--hero">
					<div class="container">
						<div class="hero-card">
							<p class="eyebrow">LCD Tracking</p>

							<h1>Sécurisez vos locations courte durée, sans débat.</h1>
							<p class="lead">
								Formalisez les points essentiels de fin de séjour et de ménage avec des validations simples, horodatées, consultables.
							</p>

							<div class="cta-row" aria-label="Actions principales">
								<a href="{{ path('app_register') }}" class="btn btn-primary">Demander un accès</a>
								<a href="#fonctionnement" class="btn btn-secondary">Voir comment ça marche</a>
							</div>

							<p class="reassurance" aria-label="Points clés">
								<span class="badge">Clair</span>
								<span class="badge">Factuel</span>
								<span class="badge">Rapide</span>
							</p>

							<div class="proofs proofs--hero" aria-label="Micro preuves">
								<div class="proof card card--compact">
									<div class="proof-icon" aria-hidden="true">✓</div>
									<div class="proof-text">Checklist courte</div>
								</div>
								<div class="proof card card--compact">
									<div class="proof-icon" aria-hidden="true">⛨</div>
									<div class="proof-text">Trace horodatée</div>
								</div>
							</div>

							{# Notes système éventuelles #}
						{# <p class="system-note system-note--success">Enregistré.</p> #}
							{# <p class="system-note system-note--warning">Non confirmé : action nécessaire.</p> #}
						</div>
					</div>
				</section>
			{% endif %}

			{% block body %}{% endblock %}
		</main>

		{{ include('partials/_footer.html.twig') }}
	</body>
</html>
